model User {
    id            Int       @id @default(autoincrement())
    name          String?
    account       String    @unique
    // checkBalance  Boolean         @default(true)
    endingBalance Decimal   @default(0)
    // records       BalanceRecord[]
    records       Balance[]
}
